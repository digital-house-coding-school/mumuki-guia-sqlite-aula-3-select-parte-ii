- type: query
  expected: SELECT DISTINCT clientes.nome, clientes.sobrenome from clientes left join vendas ON vendas.id_cliente = clientes.id left JOIN produtos ON produtos.id = vendas.id_produto where produtos.nome like "iphone%" order by sobrenome;